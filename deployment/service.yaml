apiVersion: v1
kind: Service
metadata:
  name: wize-organization-service
  namespace: wizeworks
  labels:
    app: wize-organization
spec:
  selector:
    app: wize-organization
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP